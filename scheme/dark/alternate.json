{
  "alias": {
    "color": {
      "base": {
        "background": { "value": "{alias.color.palette.white.value}" },
        "foreground": { "value": "{alias.color.palette.black.value}" },
        "indistinct": { "value": "{alias.color.palette.grey.50.value}" },
        "faint": { "value": "{alias.color.palette.grey.100.value}" },
        "soft": { "value": "{alias.color.palette.grey.200.value}" },
        "tinged": { "value": "{alias.color.palette.grey.300.value}" },
        "dim": { "value": "{alias.color.palette.grey.400.value}" },
        "neutral": { "value": "{alias.color.palette.grey.500.value}" },
        "tinted": { "value": "{alias.color.palette.grey.600.value}" }
      },
      "surface": {
        "background": { "value": "{alias.color.palette.white.value}" },
        "foreground": { "value": "{alias.color.palette.black.value}" }
      },
      "inverse": {
        "background": { "value": "{alias.color.base.foreground.value}" },
        "foreground": { "value": "{alias.color.base.background.value}" }
      },
      "cta": {
        "background": { "value": "{alias.color.palette.purple.600.value}" }
      },
      "success": {
        "background": { "value": "{alias.color.palette.green.600.value}" }
      },
      "error": {
        "background": { "value": "{alias.color.palette.red.600.value}" }
      },
      "info": {
        "background": { "value": "{alias.color.palette.blue.600.value}" }
      },
      "announcement": {
        "background": { "value": "{alias.color.palette.magenta.600.value}" }
      }
    }
  }
}

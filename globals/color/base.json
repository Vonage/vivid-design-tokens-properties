{
  "color": {
    "background": {
      "base": { "value": "{alias.color.scheme.background.base.value}" },
      "on": {
        "base": { "value": "{alias.color.scheme.background.on.base.value}" },
        "dim": { "value": "{alias.color.scheme.background.on.dim.value}" }
      }
    },
    "surface": {
      "base": { "value": "{alias.color.scheme.surface.base.value}" },
      "on": {
        "base": { "value": "{alias.color.scheme.surface.on.base.value}" },
        "dim": { "value": "{alias.color.scheme.surface.on.dim.value}" }
      }
    },
    "primary": {
      "base": { "value": "{alias.color.scheme.primary.base.value}" },
      "on": { "value": "{alias.color.scheme.primary.on.value}" }
    },
    "secondary": {
			"base": { "value": "{alias.color.scheme.secondary.base.value}" },
      "on":  { "value": "{alias.color.scheme.secondary.on.value}" }
		 }
  }
}
